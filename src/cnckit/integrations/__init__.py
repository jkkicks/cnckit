"""
Optional integration modules - loaded only on demand.

These modules provide additional functionality but require
external dependencies. Install with extras:

    pip install cnckit[api]       # REST API (FastAPI)
    pip install cnckit[mqtt]      # MQTT client
    pip install cnckit[websocket] # WebSocket server
    pip install cnckit[robot]     # Robot interfaces (ROS2, TCP)
    pip install cnckit[all]       # Everything
"""
